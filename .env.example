# ====== FLOW (Sandbox por defecto) ======
FLOW_API_KEY=tu_api_key_flow
FLOW_SECRET=tu_secret_flow
FLOW_COMMERCE_ID=99999
FLOW_BASE_URL=https://sandbox.flow.cl/api
FLOW_RETURN_URL=http://localhost:3000/flow/retorno
FLOW_CONFIRM_URL=http://localhost:3000/flow/confirm

# ====== CORS ======
# Coma-separado. Ejemplo producción: https://mision3d.cl,https://www.mision3d.cl
CORS_ORIGIN=http://localhost:8080

# ====== SMTP (opcional para emails) ======
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=tu_usuario@gmail.com
SMTP_PASS=tu_app_password
MAIL_FROM="Misión 3D <no-reply@mision3d.cl>"

# ====== SEGURIDAD (Alertas) ======
# Email para recibir alertas de seguridad (firmas inválidas, replay attacks, etc.)
SECURITY_ALERT_EMAIL=admin@mision3d.cl
# Slack webhook para alertas (opcional)
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/TU_WEBHOOK

# ====== REDIS (Recomendado para producción) ======
# Para almacenar tokens procesados de forma distribuida
# REDIS_URL=redis://localhost:6379
# REDIS_PASSWORD=tu_password_redis

